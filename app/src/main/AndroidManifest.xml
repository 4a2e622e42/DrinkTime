<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.waterremider">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="وقت نوشیدن"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.WaterRemider"
        >
        <activity
            android:name=".Activitys.SettingsActivity"
            android:screenOrientation="portrait"

            >
        </activity>

        <activity android:name=".Activitys.StartActivity"
            android:screenOrientation="portrait"

            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.MainActivity"
            android:screenOrientation="portrait"

            />

        <service  android:enabled="true"  android:name=".BroadCasts.ReminderBroadcast"/>
        <service  android:enabled="true"  android:name=".Services.SendNotification"/>
        <service  android:enabled="true"  android:name=".Services.TurnOffNotification"/>

    </application>

</manifest>